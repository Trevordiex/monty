#include <stdlib.h>
#include <stdio.h>
#include "main.h"

/**
 * push - pushes an element onto the stack
 * @stack: the next item to be pushed onto the stack
 * @line_number: the line number being processed
 *
 * Return: returns a pointer to the top
 */
void *push(stack_t **stack, unsigned int line_number)
{
	if (!top)
		printf("top is found everywhere\n");
	return (NULL);
}
